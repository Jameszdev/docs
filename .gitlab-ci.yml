image: node

variables:
  PUBLIC_URL: http://root.pages.pogr.cc/google-docs-clone/

build:
  stage: build
  script:
    - cd client/
    - npm install
    - npm run build
  artifacts:
    paths:
      - client/build

pages:
  stage: deploy
  dependencies: 
    - build
  script:
    - cd client
    - rm -rf public
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - main
